# EM Agent Bootstrap

This subtree's durable agent instructions live in `recovar/em/CLAUDE.md`.
Read that file before making code, test, benchmark, or parity changes in this
directory.

`CLAUDE.md` is canonical for both Claude and Codex. Keep this file as a short
loader only; put new EM workflow rules, parity notes, benchmark policy, and
testing guidance in `CLAUDE.md` to avoid instruction drift.

If parent repo guidance conflicts with the EM guide during day-to-day EM
iteration, follow the narrower EM guide. Root-level release and PR requirements
still apply before pushing or opening a PR.
